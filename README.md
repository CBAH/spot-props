All regions contains files that are related to the upcoming publication that analyses spot properties accross all regions in the HOYS database. 

temp_fit_stageiv_4 is the file that uses catalogue colours to fit a function to determine the approximate temperature of an object - which is needed for spot fitting. 

Spot grid to file is the file that generates a magnitude difference for a random spot temp, spot size and star temp. 
